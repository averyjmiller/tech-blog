<section class="card">
  <a href="/post/{{id}}">
    <div class="card-header">
      <h3 class="post-title">{{title}}</h3>
      <div class="post-date">Posted by {{user.username}} on {{createdAt}}</div>
    </div>
    <p class="post-content">{{content}}</p>
  </a>
</section>